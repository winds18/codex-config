# 独立任务交接

适用前提：用户明确要求一个独立用户任务；普通内部并行不使用此流程。

启动包示例：

```text
目标：<明确交付物>
项目与起点：<工具确认的项目；用户指定的起点>
已完成与证据：<需要承接的事实>
约束与接口：<不可改变的行为>
文件归属：<负责范围；共享文件负责人>
验收：<实际命令或场景>
```

调用前核实工具能力与项目；用户未指定模型时保持默认。创建后只用真实返回 ID，等待异步状态；同一状态不反复读取完整历史。

交付摘要包括实际变更、验证、限制和需接续的事项。状态、合并与归档以工具事实为准，计划文档不充当第二份应用数据库。
